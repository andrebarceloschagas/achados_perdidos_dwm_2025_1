import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'achados-perdidos-mobile',
  webDir: 'www'
};

export default config;
